---
phase: 02-content-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements:
  - CONT-02
  - CONT-03
  - CONT-04

must_haves:
  truths:
    - "index.html starts with --- as absolute first bytes so Liquid is processed"
    - "Homepage shows latest 3 posts via Jekyll Liquid loop (not 'Loading articles...')"
    - "Homepage shows 2 portfolio items via Jekyll Liquid loop (not static placeholders)"
    - "All 4 script tags and inline DOMContentLoaded block removed from index.html"
    - "Admin nav link removed from index.html"
    - "Hero section (h2, .lead) retains static hardcoded text unchanged"
  artifacts:
    - path: "index.html"
      provides: "Jekyll-processed homepage with Liquid loops for posts and portfolio"
      contains: "for post in site.posts"
  key_links:
    - from: "index.html latest-blog section"
      to: "site.posts"
      via: "{% for post in site.posts limit:3 %} Liquid loop"
      pattern: "for post in site.posts"
    - from: "index.html featured-projects section"
      to: "site.data.portfolio"
      via: "{% for item in site.data.portfolio limit:2 %} Liquid loop"
      pattern: "for item in site.data.portfolio"
---

<objective>
Rewrite index.html so the homepage latest-posts and portfolio-preview sections render via Jekyll
Liquid at build time, and remove all JS script infrastructure from the page.

Purpose: The homepage currently shows "Loading articles..." in the blog section and static
placeholder cards in the projects section. Both are populated by content-loader.js from
localStorage — invisible to visitors. This plan replaces both sections with Jekyll Liquid loops
and removes all script tags. The hero and contact sections are left unchanged (they are correct
as static HTML).

Output:
- `index.html` — rewritten to add empty frontmatter (`---\n---`) as the first two lines,
  replace the `.latest-blog .card-list` section with `{% for post in site.posts limit:3 %}`,
  replace the `.featured-projects .card-list` with `{% for item in site.data.portfolio limit:2 %}`,
  remove the Admin nav link, and remove all 5 JS script elements. The hero section
  (`<h2>Hello, I'm Kanha</h2>`, `.lead`) is kept exactly as-is.
</objective>

<execution_context>
@/Users/kanhasongeam/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kanhasongeam/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-content-migration/02-CONTEXT.md
@.planning/phases/02-content-migration/02-RESEARCH.md
@.planning/phases/02-content-migration/02-PATTERNS.md

<interfaces>
<!-- Context extracted from current index.html (read in planning) -->

Current index.html annotated structure:
  Line 1:       <!-- HTML comment (MUST be replaced — prevents Jekyll Liquid processing) -->
  Lines 2-14:   <!DOCTYPE html> through </head> — KEEP AS-IS
  Lines 16-30:  <header> with <nav> — KEEP header/nav, REMOVE Admin link (lines 25-27)
  Lines 33-42:  <section class="hero"> — KEEP ENTIRELY UNCHANGED
  Lines 44-62:  <section class="featured-projects"> with 2 static <a class="card"> placeholders
                → REPLACE .card-list content with Liquid loop (keep <section> and <h2> wrapper)
  Lines 64-72:  <section class="latest-blog"> with #homepage-blog-list div
                → REPLACE .card-list content with Liquid loop (keep <section> and <h2> wrapper)
  Lines 74-88:  <section id="contact" class="contact"> — KEEP AS-IS
                (includes placeholder github/linkedin/email links — left for Phase 3 HOME-01)
  Lines 90-95:  <footer> — KEEP AS-IS
  Lines 97-113: 4 script tags + inline DOMContentLoaded block — REMOVE ENTIRELY

Hero section to preserve verbatim (lines 33-42):
  <section class="hero">
    <div class="hero-content">
      <h2>Hello, I'm Kanha</h2>
      <p class="lead">Passionate about AI, Machine Learning, and Software Development</p>
      <div class="cta-buttons">
        <a href="portfolio/index.html" class="btn primary">View My Work</a>
        <a href="blog/index.html" class="btn secondary">Read My Blog</a>
      </div>
    </div>
  </section>

Admin nav link to REMOVE (lines 25-27):
  <a href="admin/index.html" style="color: var(--primary-color);">
    <i class="fas fa-cog"></i> Admin
  </a>

Script block to REMOVE (lines 97-113):
  <script src="assets/js/admin-config.js"></script>
  <script src="assets/js/admin-link-handler.js"></script>
  <script src="assets/js/data-manager.js"></script>
  <script src="assets/js/content-loader.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      if (typeof DataManager !== 'undefined') { DataManager.init(); }
      ContentLoader.loadBackgroundInfo();
      ContentLoader.loadHomepagePortfolio();
      ContentLoader.loadHomepageBlogPosts();
    });
  </script>

Liquid patterns from _config.yml (baseurl: "/kanha_studyjournal.github.io"):
  Post links:      {{ post.url | prepend: site.baseurl }}
  Date format:     {{ post.date | date: "%B %-d, %Y" }}
  Excerpt:         {{ post.excerpt | strip_html | truncatewords: 25 }}
  Portfolio link:  {{ item.repo | default: '#' }}

CRITICAL: index.html uses bare paths (assets/...) not relative (../assets/...) because it is at
root. CSS and relative links like href="portfolio/index.html" stay as-is — they don't need Liquid
baseurl because they are relative paths, not root-relative.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite index.html — add frontmatter, Liquid loops, remove JS and Admin link</name>
  <files>index.html</files>
  <action>
Overwrite `index.html` completely with the content below.

CRITICAL checks before writing:
1. The file MUST start with `---` as the very first two characters (empty frontmatter).
2. Do NOT add `layout: default` — the file keeps its own DOCTYPE/html/body skeleton.
3. Hero section must be kept exactly as it appears in the current file — do not modify h2 or .lead.
4. Admin nav link must NOT appear in the new file.
5. No script tags of any kind.
6. Contact section placeholder links (github.com/yourusername etc.) are kept as-is — Phase 3 will update them.
7. The featured-projects loop uses `limit:2` and the latest-blog loop uses `limit:3`.

Write this exact content:

```html
---
---
<!--
  Homepage — Jekyll Liquid renders latest posts and portfolio at build time.
  No JavaScript required for content.
-->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <meta name="description" content="Kanha's Study Journal — notes on algorithms, systems, ML, and building things." />
  <title>Kanha Songeam | AI/ML Engineer &amp; Developer</title>
  <link rel="stylesheet" href="assets/css/style.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" />
</head>
<body>
  <header>
    <div class="header-content">
      <h1>Kanha Songeam</h1>
      <p class="subtitle">AI/ML Engineer &amp; Developer</p>
      <nav>
        <a href="index.html" class="active">Home</a>
        <a href="portfolio/index.html">Portfolio</a>
        <a href="blog/index.html">Blog</a>
        <a href="#contact">Contact</a>
      </nav>
    </div>
  </header>

  <main>
    <section class="hero">
      <div class="hero-content">
        <h2>Hello, I'm Kanha</h2>
        <p class="lead">Passionate about AI, Machine Learning, and Software Development</p>
        <div class="cta-buttons">
          <a href="portfolio/index.html" class="btn primary">View My Work</a>
          <a href="blog/index.html" class="btn secondary">Read My Blog</a>
        </div>
      </div>
    </section>

    <section class="featured-projects">
      <h2>Featured Projects</h2>
      <div class="card-list">
        {% for item in site.data.portfolio limit:2 %}
          <a class="card" href="{{ item.repo | default: '#' }}">
            <div class="card-content">
              <h3>{{ item.title }}</h3>
              <p>{{ item.description }}</p>
            </div>
          </a>
        {% endfor %}
      </div>
    </section>

    <section class="latest-blog">
      <h2>Latest Articles</h2>
      <div class="card-list">
        {% for post in site.posts limit:3 %}
          <a class="card" href="{{ post.url | prepend: site.baseurl }}">
            <div class="card-content">
              <span class="date">{{ post.date | date: "%B %-d, %Y" }}</span>
              <h3>{{ post.title }}</h3>
              <p>{{ post.excerpt | strip_html | truncatewords: 25 }}</p>
              <span class="read-more">Read More &rarr;</span>
            </div>
          </a>
        {% endfor %}
      </div>
    </section>

    <section id="contact" class="contact">
      <h2>Get in Touch</h2>
      <div class="social-links">
        <a href="https://github.com/songeamkanha" target="_blank" rel="noopener noreferrer">
          <i class="fab fa-github"></i>
        </a>
        <a href="https://linkedin.com/in/kanha-songeam" target="_blank" rel="noopener noreferrer">
          <i class="fab fa-linkedin"></i>
        </a>
        <a href="mailto:kanhasongeam2309@gmail.com">
          <i class="fas fa-envelope"></i>
        </a>
      </div>
    </section>
  </main>

  <footer>
    <div class="footer-content">
      <p>&copy; 2026 Kanha Songeam. All rights reserved.</p>
    </div>
  </footer>
</body>
</html>
```

After writing the file, stage and commit:
```
git add index.html
git commit -m "feat(02-03): rewrite homepage with Jekyll Liquid post and portfolio loops"
```
  </action>
  <verify>
    <automated>head -1 index.html | grep -q "^---$" && grep -q "for post in site.posts limit:3" index.html && grep -q "for item in site.data.portfolio limit:2" index.html && ! grep -q "admin-config.js" index.html && ! grep -q "content-loader.js" index.html && ! grep -q "admin/index.html" index.html && ! grep -q "Loading articles" index.html && grep -q "Hello, I'm Kanha" index.html && echo "PASS: index.html is Jekyll Liquid homepage with hero preserved, no admin/JS references"</automated>
  </verify>
  <done>
    - `index.html` first line is `---`
    - Contains `{% for post in site.posts limit:3 %}` loop in .latest-blog section
    - Contains `{% for item in site.data.portfolio limit:2 %}` loop in .featured-projects section
    - Hero section preserved: `<h2>Hello, I'm Kanha</h2>` and `.lead` paragraph unchanged
    - "Loading articles..." placeholder gone
    - Static placeholder project cards gone
    - All 4 script tags removed
    - Inline DOMContentLoaded block removed
    - Admin nav link removed
    - File committed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| site.posts → homepage Liquid | Post frontmatter values (title, excerpt, date) embedded in index.html at build time |
| site.data.portfolio → homepage Liquid | Portfolio YAML values embedded in index.html at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | index.html | accept | Version-controlled; any change is a tracked git commit |
| T-02-03-02 | Information Disclosure | post.excerpt in homepage | accept | Excerpts are intentionally public, authored in frontmatter |
| T-02-03-03 | Spoofing | contact links (email, github, linkedin) | accept | Hardcoded to real user accounts; no runtime input |
</threat_model>

<verification>
After task completes:

```bash
# Frontmatter is first
head -1 index.html | grep -q "^---$"

# Liquid loops present
grep -q "for post in site.posts limit:3" index.html
grep -q "for item in site.data.portfolio limit:2" index.html

# No JS or admin remnants
! grep -q "script" index.html
! grep -q "admin" index.html
! grep -q "Loading" index.html

# Hero preserved
grep -q "Hello, I'm Kanha" index.html

# Committed
git log --oneline -2
```
</verification>

<success_criteria>
- `index.html` has `---` as its absolute first line (empty frontmatter)
- `.latest-blog` section contains `{% for post in site.posts limit:3 %}` — no more "Loading articles..." placeholder
- `.featured-projects` section contains `{% for item in site.data.portfolio limit:2 %}` — no more static icon cards
- Hero section is intact and unchanged: `<h2>Hello, I'm Kanha</h2>` and `.lead` text present
- Zero script tags in the file
- Admin nav link absent
- File committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-migration/02-03-SUMMARY.md`
</output>
